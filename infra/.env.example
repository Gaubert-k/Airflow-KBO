# Copier vers infra/.env pour Docker Compose :
#   cp infra/.env.example infra/.env

# --- Identité Airflow (image officielle) ---
AIRFLOW_UID=50000
AIRFLOW_GID=0
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# --- Chemins projet (montés dans les conteneurs) ---
AIRFLOW_PROJ_DIR=/home/guillaume/Bureau/Airflow TP
AIRFLOW_DAGS_DIR=${AIRFLOW_PROJ_DIR}/airflow/dags
AIRFLOW_PACKAGES_DIR=${AIRFLOW_PROJ_DIR}/packages

# --- Core Airflow ---
AIRFLOW__CORE__LOAD_EXAMPLES=False
AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=False
AIRFLOW__CORE__EXECUTOR=LocalExecutor

# --- PYTHONPATH pour importer packages/ depuis les tasks ---
PYTHONPATH=/opt/airflow

# --- URLs publiques ---
KBO_PUBLIC_BASE_URL=https://kbopub.economie.fgov.be/kbopub/
BNB_CONSULT_BASE_URL=https://consult.cbso.nbb.be/
MONITEUR_EJUSTICE_BASE_URL=https://www.ejustice.just.fgov.be/cgi_tsv/
# Statuts : vide = Moniteur list.pl (sujet PDF) ; décommenter pour notaire.be (consigne prof)
# NOTAIRE_STATUTES_URL=https://www.notaire.be/entreprendre/demarrer-une-entreprise/comment-creer-une-societe-etapes-suivre/les-statuts-dune-societe

# --- HDFS / stockage brut (MS-03) ---
HDFS_RAW_ROOT=hdfs://localhost:9000/raw/v1
HDFS_LOCAL_ROOT=/opt/airflow/data/hdfs-stub

# --- Base métier (MS-05) ---
# Conteneur : hostname postgres. Hôte (tests locaux) : localhost:5432
DATABASE_URL=postgresql://app:app@postgres:5432/belgian_companies

# --- Bootstrap premier démarrage (volume data/ vide) ---
# PLATFORM_AUTO_BOOTSTRAP=1
# PLATFORM_BOOTSTRAP_SCRAPE_BATCH=2000
# PLATFORM_BOOTSTRAP_SCRAPE_MAX_BATCHES=0

# --- Acquisition (MS-02) — Tor KBO (recommandé) ---
# ACQUISITION_USE_TOR=1
# ACQUISITION_TOR_PROXIES=socks5h://tor1:9050,socks5h://tor2:9050,socks5h://tor3:9050
# ACQUISITION_KBO_DIRECT_FIRST=1
# ACQUISITION_TOR_LOOP=0
# ACQUISITION_TOR_LOOP_MIN_INTERVAL_S=600
# ACQUISITION_HTTP_TIMEOUT_S=60
# TOR_CONTROL_PASSWORD=mypass
# --- Proxies HTTP fichier (legacy, optionnel) ---
# ACQUISITION_USE_PROXIES=0
# ACQUISITION_PROXY_FILE=/opt/airflow/data/proxies/proxies.txt
# ACQUISITION_WORKER_ID=acquisition-worker-1

# --- Pip additionnel image Airflow (MS-05 / MS-10) ---
# AIRFLOW_PIP_ADDITIONAL_REQUIREMENTS=sqlalchemy==2.0.36 psycopg2-binary==2.9.10
